import styled from 'styled-components';

export const BookFullInfoContainer = styled('div')({
    backgroundColor: 'rgba(98, 98, 66, 0.23)',
    maxWidth: 900,
    maxHeight: 1200,
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    flexDirection: 'column',
})

export const BookFullInfoWrapper = styled('div')({
    backgroundColor: '#d14d4d',
    display: 'flex',
    justifyContent: 'center',
    alignItems: 'center',
    flexDirection: 'column',
})
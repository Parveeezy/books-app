import React from 'react';
import { CustomButton } from './components';

const CustomBtn = () => {
    return (
        <CustomButton>
            Search
        </CustomButton>
    );
};

export default CustomBtn;